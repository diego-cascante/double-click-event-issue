import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Double Click Event Issue',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
